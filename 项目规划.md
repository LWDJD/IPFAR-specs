# IPFAR 项目规划

## 依赖库

| 用途 | 库名 / 链接 |
|------|------------|
| 配置文件管理 | [knadh/koanf](https://github.com/knadh/koanf) |
| KV数据库 | [dgraph-io/badger](https://github.com/dgraph-io/badger) |
| 多语言支持 | [leonelquinteros/gotext](https://github.com/leonelquinteros/gotext) |
| 多系统语言获取POSIX格式 | [Xuanwo/go-locale](https://github.com/Xuanwo/go-locale) |
| CAR文件解析 | [ipld/go-car](https://github.com/ipld/go-car) |
| BitSwap支持 | [ipfs/boxo](https://github.com/ipfs/boxo) |
| 点对点支持 | [libp2p/go-libp2p](https://github.com/libp2p/go-libp2p) |
| 内容发布及寻址支持 | [libp2p/go-libp2p-kad-dht](https://github.com/libp2p/go-libp2p-kad-dht) |
| Arweave ANS-104 捆绑包阅读与验证 | [ar-io/arbundles](https://github.com/ar-io/arbundles) 或 [permadao/goar](https://github.com/permadao/goar) |

## 一、数据格式

1. Arweave上的文件采用：CID元数据(带Tags标签)→CAR文件(V2)。**CAR V2 必须带索引（Index），否则拒绝。** 元数据中包含可选的DAG引用项目(目的是可选的去重)。

> 详细格式定义见 `数据结构规范.md`：
> - Tag 标准：一、Arweave Transaction Tags
> - 元数据 JSON：二、元数据 JSON 格式
> - CAR v2 规范：三、CAR v2 文件格式
> - 引用机制：四、引用（Reference）

## 二、验证策略

1. 主CAR文件大于等于100 MiB的文件无PoW验证，采用被动验证。
2. 主CAR文件小于100 MiB的文件使用快速PoW验证缓解，采用被动验证。
3. 开发全量验证功能，这适合重量级桥。

> **经济缓解**：同时配合存储费攻击缓解方案（见安全与经济章节）。小于100 MiB的文件依靠存储费+PoW缓解；大于等于 100 MiB的文件依靠存储费缓解。

### 2.1 工作量证明（PoW）

#### 设计目的

防止垃圾数据攻击。**小于 100 MiB 的文件需 PoW 验证，大于等于 100 MiB 的文件免 PoW**，使攻击者无法低成本批量上传小文件。

#### 算法

| 参数 | 值 |
|------|-----|
| 算法标识 | `argon2id-light-v1` |
| 哈希函数 | Argon2id（内存硬函数，抗 ASIC/GPU/量子） |
| 密码（password） | `root_cid + data_txid`（字符串拼接） |
| 盐（salt） | `pow` 字段的值（64 位无符号整数，十进制字符串） |
| Argon2id 参数 | 内存 20MB，时间 1，并行 1（全档位统一） |
| 计算方式 | 从 salt = 0 开始递增，计算 `Argon2id(password, salt, 20MB/1/1)`。当输出哈希的前导零满足难度要求时，将当前的 salt 值写入 `pow` 字段 |
| `pow` 字段 | 满足难度要求的 salt 值（十进制字符串） |

**参数说明**：
- 20MB 内存 + 1 次迭代：验证时间 ~20ms，同时对 ASIC 不友好（ASIC 需要为每个核心配备独立 20MB SRAM，面积成本高）
- 所有档位使用相同参数，只通过前导零位数控制难度
- 简单、统一、可预测

#### 难度曲线

难度根据 `data_txid` 对应文件在 Arweave 上占用的数据大小计算：

| 文件大小（Arweave data_size） | 前导零要求 | 平均计算次数 |
|------------------------------|-----------|-------------|
| < 100 MiB | 前导零 ≥ 2 字节（16 位） | ~65,536 次 |
| ≥ 100 MiB | 无 PoW 要求 | - |

#### 验证

桥节点收到元数据时：
1. 读取 `pow_alg` 确认算法标识
2. 根据 `data_txid` 对应的 Arweave 数据大小确认文件是否 < 100 MiB，若是则要求前导零 ≥ 2 字节（16 位）
3. 使用固定 Argon2id 参数（20MB/1/1）验证：`Argon2id(root_cid+data_txid, pow)` → 检查输出前导零
4. 不通过则拒绝该元数据

**单次验证耗时：~1ms**

**被动验证**：桥节点不主动触发验证流程，仅在收到数据请求或满足触发条件时按可配置验证选项（§4.1）执行对应检查。未开启的验证项直接跳过，不产生额外开销。

## 三、数据发现

1. 采用去重随机抽样的方式发现 IPFS 数据所在区块，同时订阅或轮询最新块。
2. 使用 bitlist 标记块高是否存在 IPFS 数据，每一位表示一个块高度，0 表示无，1 表示有，配合不可信分发机制分发。由于 bitlist 极小，可以轻易存储几十上百个不可信导航。

### 3.1 去重随机抽样模式（基础）

通过去重随机抽样定位 IPFS 数据所在的区块，使用可选的 bitlist 引导标记块高是否存在 IPFS 数据，配合不可信分发机制实现去中心化发现。

**数据获取来源**：随机抽样定位到目标区块后，通过任意 API 或网关获取该区块数据。API 来源的可靠性与安全性由使用者自行负责，最终数据通过 IPFS CID 密码学验证兜底。

### 3.2 GraphQL 模式（高级，实验性）

```
GraphQL 搜索 Protocol Tag → 获取元数据交易 → 解析 JSON → 获取 data_txid → 通过 reference 下载所有分块
```


## 四、验证与桥节点行为

1. Bitswap使用延迟回复机制降低自己被使用的优先级，减少arweave回源次数。
2. 开发本地文件缓存功能，这时候所有桥，可自定义缓存大小。
3. 可自定义网关接口，以便用户可以根据需要使用公共网关、本地网关、可验证网关路由等方式，最终安全性由IPFS兜底。
4. 需要同时原生数据和Bundle捆绑包中的数据。

### 4.1 可配置验证选项

桥节点在发现流程中对各项验证均可独立开关，节点运营者可根据自身安全需求与资源预算自由配置。**注意：元数据合法性（JSON 格式、必填字段、Tag 标准）始终强制校验，不属于可配置项——任何不合法的元数据直接拒绝。**

所有可配置验证分为四项独立验证项，每项一个配置键：

#### 验证项清单

**1. 工作量验证（PoW）**

| 属性 | 说明 |
|------|------|
| 配置键 | `verify_pow` |
| 验证内容 | 验证 Argon2id PoW 是否满足难度要求（按 §2.1 流程） |
| 关闭行为 | 跳过 PoW 检查 |

**2. Index 完整性**

| 属性 | 说明 |
|------|------|
| 配置键 | `verify_index` |
| 验证内容 | 验证 CAR v2 Index 完整性（见 `数据结构规范.md` §3.4） |
| 关闭行为 | 跳过 Index 内容校验（完整性检查），但 Index 存在性检查仍强制执行 |

**3. 引用链验证**

| 属性 | 说明 |
|------|------|
| 配置键 | `verify_reference_chain` |
| 验证内容 | 验证引用链（reference chain）的完整解析（见 `数据结构规范.md` §4.6） |
| 关闭行为 | 跳过引用链验证 |

**4. 数据完整性验证**

| 属性 | 说明 |
|------|------|
| 配置键 | `verify_integrity` |
| 验证内容 | 对下载的 CAR 文件做 IPFS 数据完整性验证，重算哈希并与 CID 比对 |
| 关闭行为 | 跳过数据完整性验证 |

#### 配置方式

验证选项在**桥节点本地配置**中设置（非链上），运营者通过 JSON 配置文件逐项控制：

```json
{
  "verify_pow": true,
  "verify_index": true,
  "verify_reference_chain": true,
  "verify_integrity": true
}
```

关闭某项验证意味着桥节点**跳过**对应步骤，直接进入下一流程。关闭验证可能降低安全性，运营者应自行评估风险。

#### 安全等级预设

为简化配置，提供四档预设等级：

| 等级 | `verify_pow` | `verify_index` | `verify_reference_chain` | `verify_integrity` | 说明 |
|------|-----------|-------------|----------------------|------------------|------|
| `strict` | ✅ | ✅ | ✅ | ✅ | 全开，最安全 |
| `balanced` | ✅ | ✅ | ❌ | ✅ | 均衡模式 |
| `light` | ✅ | ❌ | ✅ | ❌ | 仅验证 PoW 与引用链（默认） |
| `trusted` | ❌ | ❌ | ❌ | ❌ | 仅验证元数据，适合开发测试 |

#### 验证流程

在发现流程（§3.1、§3.2）中，各验证步骤按以下条件执行：

```
获取元数据交易
  → 元数据合法性校验（强制，不可配置）
  → verify_pow? ─── 是 → PoW 验证（§2.1）
  → 下载主 CAR 文件
  → verify_index? ─── 是 → 验证 Index 完整性（数据结构规范 §3.4）
  → verify_reference_chain? ─── 是 → 引用链解析（数据结构规范 §4.6）
  → verify_integrity? ─── 是 → 数据哈希比对 CID
  → 收录
```

任一验证失败（开启状态下）均导致该条记录被拒绝。

## 五、安全与经济

### 垃圾数据攻击缓解方案

- 所有上传需支付 Arweave 存储费；
- 小于 100 MiB 的文件依靠存储费 + PoW 双重防护（PoW 按难度曲线 §2.1 执行）；
- 大于等于 100 MiB 的文件依靠存储费缓解（PoW 可省略，由 `verify_pow` 配置控制）；
- 桥节点采用均态分布的方式收集数据，默认采用被动验证模式，数据验证由可配置验证选项控制（见 §4.1）；
- 采用尽力而为的DHT发布方式，每个文件都可能会被发布，降低冷数据无法访问的可能性；
- 采用完全发布的IPNI发布方式，收集到的数据立刻发布到IPNI（PASS 已验证，目前无法实现：IPNI 不是 Kubo 默认路由 — Kubo 默认走 DHT，要手动配 Routing.Type=custom 才能查 IPNI，生态兼容性差；Kubo 不跟 HTTP 302 重定向 — trustless gateway 客户端收到 302 直接判 HTTP Found: No，不走重定向）。

## 六、可能相关的内容（仅供参考）

以下内容与本项目可能有关，但尚未验证是否适用：

1. zkIFPS：
   - https://zkipfs.vercel.app/
   - https://x.com/weave_db/status/1970853988045320204
   - https://www.arweaveoasis.com/new/279c18e6-90da-801a-b0ec-d37f6d23c48f
2. arweave-ipfs-bridge：https://github.com/AndreiD/arweave-ipfs-bridge