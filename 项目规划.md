# IPFAR 项目规划

## 依赖库

| 用途 | 库名 / 链接 |
|------|------------|
| 配置文件管理 | [knadh/koanf](https://github.com/knadh/koanf) |
| KV数据库 | [dgraph-io/badger](https://github.com/dgraph-io/badger) |
| 多语言支持 | [leonelquinteros/gotext](https://github.com/leonelquinteros/gotext) |
| 多系统语言获取POSIX格式 | [Xuanwo/go-locale](https://github.com/Xuanwo/go-locale) |
| CAR文件解析 | [ipld/go-car](https://github.com/ipld/go-car) |
| BitSwap支持 | [ipfs/boxo](https://github.com/ipfs/boxo) |
| 点对点支持 | [libp2p/go-libp2p](https://github.com/libp2p/go-libp2p) |
| 内容发布及寻址支持 | [libp2p/go-libp2p-kad-dht](https://github.com/libp2p/go-libp2p-kad-dht) |
| Arweave ANS-104 捆绑包阅读与验证 | [ar-io/arbundles](https://github.com/ar-io/arbundles) 或 [permadao/goar](https://github.com/permadao/goar) |

## 一、数据格式

1. Arweave上的文件采用：CID元数据(带Tags标签)→CAR文件(V2)。**CAR V2 必须带索引（Index），否则拒绝。** 元数据中包含可选的DAG引用项目(目的是可选的去重)。

> 详细格式定义见 `数据结构规范.md`：
> - Tag 标准：一、Arweave Transaction Tags
> - 元数据 JSON：二、元数据 JSON 格式
> - CAR v2 规范：三、CAR v2 文件格式
> - 引用机制：四、引用（Reference）

## 二、验证策略

3. 主CAR文件大于100 MiB的文件无PoW验证，采用被动验证。
4. 主CAR文件小于100 MiB的文件使用快速PoW验证缓解，采用被动验证。
5. 开发全量验证功能，这适合重量级桥。

> **经济缓解**：同时配合存储费攻击缓解方案（见第六章）。小于100 MiB的文件依靠存储费+PoW缓解；大于100 MiB的文件依靠存储费缓解。

### 2.1 工作量证明（PoW）

#### 设计目的

防止垃圾数据攻击。**文件越小，PoW 难度越高**，使攻击者无法低成本批量上传小文件。

#### 算法

| 参数 | 值 |
|------|-----|
| 算法标识 | `argon2id-light-v1` |
| 哈希函数 | Argon2id（内存硬函数，抗 ASIC/GPU/量子） |
| 密码（password） | `root_cid + data_txid`（字符串拼接） |
| 盐（salt） | `pow` 字段的值（64 位无符号整数，十进制字符串） |
| Argon2id 参数 | 内存 1MB，时间 1，并行 1（全档位统一） |
| 计算方式 | 从 salt = 0 开始递增，计算 `Argon2id(password, salt, 1MB/1/1)`。当输出哈希的前导零满足难度要求时，将当前的 salt 值写入 `pow` 字段 |
| `pow` 字段 | 满足难度要求的 salt 值（十进制字符串） |

**参数说明**：
- 1MB 内存 + 1 次迭代：验证时间 ~1ms，同时对 ASIC 不友好（ASIC 需要为每个核心配备独立 1MB SRAM，面积成本高）
- 所有档位使用相同参数，只通过前导零位数控制难度
- 简单、统一、可预测

#### 难度曲线

难度根据 `data_txid` 对应文件在 Arweave 上占用的数据大小计算：

| 文件大小（Arweave data_size） | 前导零要求 | 平均计算次数 |
|------------------------------|-----------|-------------|
| < 100 MiB | 前导零 ≥ 2 字节（16 位） | ~65,536 次 |
| ≥ 100 MiB | 无 PoW 要求 | - |

#### 验证

桥节点收到元数据时：
1. 读取 `pow_alg` 确认算法标识
2. 根据 `data_txid` 对应的 Arweave 数据大小确定所需前导零位数
3. 使用固定 Argon2id 参数（1MB/1/1）验证：`Argon2id(root_cid+data_txid, pow)` → 检查输出前导零
4. 不通过则拒绝该元数据

**单次验证耗时：~1ms**

## 三、发现与获取

9. 从矿工获取原始数据，而是直接询问矿工，在块中搜索，采用去重随机游走的方式寻找数据，同时保证订阅或轮询最新的块。
11. 使用带导航的随机游走，使用bitlist存储块高是否存在IPFS数据即可，每一位表示一个块高度，0表示无，1表示有，采用某种不可信的分发方式，由于bitlist极小，所以可以轻易存储几十上百个不可信导航。

### 3.1 GraphQL 模式（基础）

```
GraphQL 搜索 Protocol Tag → 获取元数据交易
  → 解析 JSON → 获取 data_txid → 下载主 CAR 文件
  → 验证 data_size 是否 < 100 MiB
    ├── 是 → 验证元数据中的 PoW（见 §2.1）
    └── 否 → 跳过 PoW 验证
  → 读取 CAR v2 Header → 获取根 CID → 比对 root_cid
    ├── 一致 → 通过 ✅
    └── 不一致 → 拒绝 ❌
  → 通过 reference 下载所有分块 → 合并为完整 DAG
```

**防重复机制**：按 §5.1 规则处理重复 CID。

### 3.2 矿工直询模式（高级，实验性）

通过去重随机游走直接向 Arweave 矿工查询区块数据，使用 bitlist 标记块高是否存在 IPFS 数据，配合不可信分发机制实现去中心化发现。此模式不依赖 GraphQL 或网关。

## 四、桥节点行为

2. Bitswap使用延迟回复机制降低自己被使用的优先级，减少arweave回源次数。
6. 开发本地文件缓存功能，这时候所有桥，可自定义缓存大小。
8. 可自定义网关接口，以便用户可以根据需要使用公共网关、本地网关、可验证网关路由等方式，最终安全性由IPFS兜底。
12. 需要同时原生数据和Bundle捆绑包中的数据。

### 4.1 可配置验证选项

桥节点在发现流程中对各项验证均可独立开关，节点运营者可根据自身安全需求与资源预算自由配置。**注意：元数据合法性（JSON 格式、必填字段、Tag 标准）始终强制校验，不属于可配置项——任何不合法的元数据直接拒绝。**

所有可配置验证分为三大类：

#### 验证项清单

**1. 工作量验证（PoW）**

| 属性 | 说明 |
|------|------|
| 配置键 | `验证工作量` |
| 验证内容 | 验证 Argon2id PoW 是否满足难度要求（按 §2.1 流程） |
| 关闭行为 | 跳过 PoW 检查 |

**2. 数据头验证（CAR Header）**

| 属性 | 说明 |
|------|------|
| 配置键 | `验证数据头` |
| 验证内容 | ① 验证 CAR v2 文件头的根 CID 是否与元数据中的 `root_cid` 一致；② 验证 CAR v2 Index 完整性（见 `数据结构规范.md` §3.4） |
| 关闭行为 | 跳过 CAR 头部检查 |

**3. 数据合法性验证**

| 属性 | 说明 |
|------|------|
| 配置键 | `验证数据合法性` |
| 验证内容 | ① 验证引用链（reference chain）的完整解析（见 `数据结构规范.md` §4.6）；② 重复 CID 检测（见 §5.1） |
| 关闭行为 | 跳过数据合法性验证，直接收录数据 |

#### 配置方式

验证选项在**桥节点本地配置**中设置（非链上），运营者通过 JSON 配置文件逐项控制：

```json
{
  "验证工作量": true,
  "验证数据头": true,
  "验证数据合法性": true
}
```

关闭某项验证意味着桥节点**跳过**对应步骤，直接进入下一流程。关闭验证可能降低安全性，运营者应自行评估风险。

#### 安全等级预设

为简化配置，提供四档预设等级：

| 等级 | `验证工作量` | `验证数据头` | `验证数据合法性` | 说明 |
|------|:-----------:|:------------------:|:-------------:|------|
| `strict` | ✅ | ✅ | ✅ | 全开，最安全 |
| `balanced` | ✅ | ✅ | ❌ | **默认推荐**，跳过数据合法性验证 |
| `light` | ❌ | ✅ | ❌ | 仅验证 CAR 头部 |
| `trusted` | ❌ | ❌ | ❌ | 仅验证元数据合法性，适合开发测试 |

#### 验证流程

在发现流程（§3.1、§3.2）中，各验证步骤按以下条件执行：

```
获取元数据交易
  → 元数据合法性校验（强制，不可配置）
  → 验证工作量? ─── 是 → PoW 验证（§2.1）
  → 下载主 CAR 文件
  → 验证数据头? ─── 是 → 比对 Header root_cid + 验证 Index 完整性（数据结构规范 §3.4）
  → 验证数据合法性? ─── 是 → 引用链解析（数据结构规范 §4.6） + 重复 CID 检测（§5.1）
  → 收录
```

任一验证失败（开启状态下）均导致该条记录被拒绝。

## 五、去重与存储

7. 开发链上增量数据库，仅桥节点运营者自己使用，可以把已经验证过的数据写入桥上，未来可以直接调用此数据，无需二次验证。
10. 去重方法采用 无状态密码学排列（Feistel 网络 / 格式保留加密）。

### 5.1 重复 CID 处理

#### 发现重复 CID

当检测到 `root_cid` 已存在时：

```
发现根 CID 已存在
  ├── data_txid 不同 → 将新元数据添加到该 CID 的元数据集合中
  └── data_txid 相同
        ├── reference 列表不同 → 添加到该 CID 的元数据集合
        └── reference 列表相同 → 完全重复，忽略
```

#### 元数据集合

一个 CID 可能对应多个元数据交易（不同上传者、不同分块方式）。桥节点维护该集合，选择可信度最高的元数据提供数据服务。

## 六、安全与经济

垃圾数据攻击缓解方案：

- 小于100 MiB的文件依靠存储费+PoW缓解；
- 大于100 MiB的文件依靠存储费缓解；
- 桥节点采用均态分布的方式收集数据，采用被动验证模式，仅在收到数据请求时验证数据是否合法；
- 采用尽力而为的DHT发布方式，每个文件都可能会被发布，降低冷数据无法访问的可能性；
- 采用完全发布的IPNI发布方式，收集到的数据立刻发布到IPNI（PASS 已验证，目前无法实现：IPNI 不是 Kubo 默认路由 — Kubo 默认走 DHT，要手动配 Routing.Type=custom 才能查 IPNI，生态兼容性差；Kubo 不跟 HTTP 302 重定向 — trustless gateway 客户端收到 302 直接判 HTTP Found: No，不走重定向）。

### 6.1 存储费与经济缓解

- 所有上传需支付 Arweave 存储费
- 小于 100 MiB 的文件依靠存储费 + PoW 双重防护
- 大于等于 100 MiB 的文件依靠存储费缓解（PoW 可省略）

## 七、可能相关的内容（仅供参考）

以下内容与本项目可能有关，但尚未验证是否适用：

1. zkIFPS：
   - https://zkipfs.vercel.app/
   - https://x.com/weave_db/status/1970853988045320204
   - https://www.arweaveoasis.com/new/279c18e6-90da-801a-b0ec-d37f6d23c48f
2. arweave-ipfs-bridge：https://github.com/AndreiD/arweave-ipfs-bridge

